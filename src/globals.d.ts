/** Injected at build time by tsup (`define`) from package.json `version`. */
declare const __WT_VERSION__: string;

/** Injected at build time by tsup (`define`) from SKILL.md. */
declare const __WT_SKILL__: string;
